@library('jenkins-shared-library')

pipeline {
    agent any
    stages {
        stage("hello world") {
            steps {
              script {
                hello.world()
                }  
            }
        }
    }
}